pipeline {
    agent any 

    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                // Yahan aap build commands likh sakte hain, jaise:
                // sh 'mvn clean package' (Java ke liye)
            }
        }
        stage('Test') {
            steps {
                echo 'Testing...'
                // Yahan aap test commands likh sakte hain, jaise:
                // sh 'mvn test' (Java ke liye)
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying...'
                // Yahan aap deployment commands likh sakte hain
            }
        }
    }
}
